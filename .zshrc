# Homebrew update shortcut
alias brewup='brew update && brew upgrade && brew cleanup'

# Prompt line
NEWLINE=$'\n'
PROMPT="${NEWLINE}%F{#FFE6B7}%D{%_I:%M%p}%F{#FAB28E} %n%F{#E95379} %~ %F{#F43E5C}❯%f"

source /Users/macchi/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh